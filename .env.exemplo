# Copie para .env e preencha. O .env esta no .gitignore - nunca comite.
# A service role key fica SO aqui: ela ignora a RLS e pode gravar em tudo.
# Pegue em: Dashboard > Project Settings > API keys > service_role (secret)

SUPABASE_URL=https://frsidsdrexolvcfxzroe.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# A chave abaixo e publica e pode aparecer no front (so le, respeitando a RLS).
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xgr6ki59dLrTcdjDlmEtZA_LAF6m1BC
